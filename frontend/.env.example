# Any VITE_-prefixed variable gets bundled straight into the public JS
# shipped to the browser - never put anything secret in here, only the
# API's public base URL.
VITE_API_BASE_URL=http://localhost:8000
